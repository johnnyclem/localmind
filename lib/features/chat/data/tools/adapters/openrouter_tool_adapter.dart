import 'package:localmind/features/chat/data/tools/adapters/openai_tool_adapter.dart';

class OpenRouterToolAdapter extends OpenAiToolAdapter {
  // OpenRouter uses identical function calling protocol to OpenAI
  // No override needed — inherits all behavior
}
